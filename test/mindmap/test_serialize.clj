(ns mindmap.test-serialize
  (:require [clojure.test :refer :all]
            ))

; TODO Working on getting basic testing working!
(testing "Basic math is sane?"
  (with-test
    (is (= 4 (+ 2 2)))))


(run-tests 'mindmap.test-serialize)
